namespace Breakout.BreakoutStates {
    public enum GameStateType {
        MainMenu,
        LevelSelector,
        GameRunning,
        GamePaused,
        GameWin,
        GameOver,
        Invalid,
    }
}
