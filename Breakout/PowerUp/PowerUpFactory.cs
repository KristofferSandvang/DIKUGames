
using System.IO;
using DIKUArcade.Entities;
using Breakout.Blocks;

namespace Breakout.PowerUps {
    public class PowerUpFactory {
        public static void SpawnPowerUp(BreakoutBlock block, PowerUpType powerUp) {
            DynamicShape shape = block.shape;
            switch (powerUp) {
                default:
                    break;
            }
        }
    }
}
